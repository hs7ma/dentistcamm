# خطة تنفيذ وحدة الأحواض الزراعية

## 📋 المتطلبات والمواصفات

### المكونات المادية:
1. **ESP32** - المتحكم الرئيسي
2. **حساس رطوبة التربة** (Soil Moisture Sensor) - قراءة رطوبة التربة
3. **حساس تدفق الماء** (Water Flow Sensor) - قياس كمية المياه المستهلكة
4. **4 ريلايهات** (كلها تعمل على 12V):
   - ريلي 1: التحكم بالمضخة (12V)
   - ريلي 2: فتح القفل/الصمام (12V)
   - ريلي 3: إغلاق القفل/الصمام (12V)
   - ريلي 4: التحكم بتشغيل حساس التدفق (12V) - Normally Closed (NC)
5. **شاشة عرض محلية** (OLED SSD1306/SSD1309 أو LCD I2C) - عرض البيانات المحلية
6. **مصادر طاقة**:
   - مصدر 12V: لتغذية المضخة، القفل، حساس التدفق، والريلايهات
   - مصدر 3.3V/5V: من ESP32 لتغذية حساس رطوبة التربة

### أنظمة الري المطلوبة:
1. **الري الكمي**: ري بكمية محددة من الماء (مثال: 5 لتر)
2. **الري الوقتي**: ري لمدة زمنية محددة (مثال: 10 دقائق)
3. **الري حسب رطوبة التربة**: ري تلقائي عند انخفاض الرطوبة تحت عتبة معينة
4. **التحكم اليدوي**: تشغيل/إيقاف يدوي من الويب

---

## 🎯 ما سأقوم بتنفيذه

### 1. البرنامج الثابت (Firmware) - ESP32

#### الملف: `firmware/esp32_plant_bed_unit.ino`

**الوظائف الرئيسية:**

1. **قراءة الحساسات:**
   - قراءة رطوبة التربة (0-100%)
   - قراءة تدفق الماء (لتر/دقيقة)
   - حساب إجمالي المياه المستهلكة (لتر)

2. **التحكم بالريلايهات:**
   - ريلي المضخة: تشغيل/إيقاف (12V)
   - ريلي فتح القفل: فتح الصمام (12V)
   - ريلي إغلاق القفل: إغلاق الصمام (12V)
   - ريلي حساس التدفق: تشغيل/إيقاف حساس التدفق (12V) - Normally Closed
   - حماية: منع فتح وإغلاق القفل في نفس الوقت
   - منطق حساس التدفق: الحساس يعمل فقط عندما ريلي 4 مغلق (NC)

3. **أنظمة الري (قابلة للتخصيص من المستخدم):**
   - **الري الكمي**: 
     - المستخدم يحدد الكمية باللتر (قابل للتعديل)
     - فتح القفل → تشغيل المضخة → مراقبة التدفق حتى الوصول للكمية المحددة → إيقاف المضخة → إغلاق القفل
     - يمكن حفظ القيمة كإعداد افتراضي
   - **الري الوقتي**:
     - المستخدم يحدد المدة بالدقائق (قابل للتعديل)
     - فتح القفل → تشغيل المضخة → انتظار المدة المحددة → إيقاف المضخة → إغلاق القفل
     - يمكن حفظ القيمة كإعداد افتراضي
   - **الري حسب الرطوبة**:
     - المستخدم يحدد عتبة الرطوبة (%) (قابل للتعديل)
     - المستخدم يختار نوع الري عند انخفاض الرطوبة (كمي أو وقتي)
     - المستخدم يحدد قيمة الري (لتر أو دقيقة حسب الاختيار)
     - مراقبة رطوبة التربة → عند انخفاضها تحت العتبة المحددة → تشغيل الري المحدد
     - يمكن حفظ جميع الإعدادات كقيم افتراضية
   - **التحكم اليدوي**:
     - تنفيذ أوامر فورية من الخادم

4. **الاتصال بالخادم:**
   - إرسال البيانات كل 5 ثوانٍ: رطوبة التربة، تدفق الماء، إجمالي الاستهلاك
   - استقبال أوامر التحكم من الخادم كل 3 ثوانٍ
   - وضع محلي عند انقطاع الاتصال

5. **عرض البيانات محلياً:**
   - عرض رطوبة التربة
   - عرض كمية المياه المستهلكة
   - عرض حالة الري (جاري/متوقف)
   - عرض حالة الاتصال

**المكتبات المطلوبة:**
- WiFi.h
- HTTPClient.h
- ArduinoJson.h
- Wire.h
- Adafruit_SSD1306.h (للشاشة OLED) أو LiquidCrystal_I2C.h (للشاشة LCD)

---

### 2. تحديث الخادم (Backend) - Node.js

#### تحديث `server.js`:

**إضافة API endpoints جديدة:**

1. **`POST /api/units/:unitId/sensor`**
   - استقبال بيانات وحدة الأحواض:
     ```json
     {
       "soilMoisture": 45.5,
       "waterFlow": 2.3,
       "totalWaterConsumed": 125.8,
       "deviceId": "plant-bed-01"
     }
     ```

2. **`GET /api/units/:unitId/control`**
   - إرسال أوامر التحكم للوحدة:
     ```json
     {
       "irrigationMode": "quantitative", // "quantitative" | "temporal" | "moisture" | "manual" | "off"
       "quantitativeValue": 5.0,        // لتر (للري الكمي) - قابل للتعديل من المستخدم
       "temporalValue": 600,            // ثانية (للري الوقتي) - قابل للتعديل من المستخدم
       "moistureThreshold": 40.0,       // % (للري حسب الرطوبة) - قابل للتعديل من المستخدم
       "moistureIrrigationType": "quantitative", // "quantitative" | "temporal" (نوع الري عند انخفاض الرطوبة)
       "moistureIrrigationValue": 5.0,  // قيمة الري عند انخفاض الرطوبة (لتر أو ثانية)
       "manualPump": false,             // true/false (للتحكم اليدوي)
       "manualValveOpen": false,        // true/false (للتحكم اليدوي)
       "manualValveClose": false        // true/false (للتحكم اليدوي)
     }
     ```

3. **`PUT /api/units/:unitId/settings`**
   - حفظ إعدادات الوحدة (قابلة للتعديل من المستخدم):
     ```json
     {
       "quantitativeDefault": 5.0,     // القيمة الافتراضية للري الكمي (لتر)
       "temporalDefault": 600,          // القيمة الافتراضية للري الوقتي (ثانية)
       "moistureThreshold": 40.0,      // عتبة الرطوبة للري التلقائي (%)
       "moistureIrrigationType": "quantitative", // نوع الري عند انخفاض الرطوبة
       "moistureIrrigationValue": 5.0  // قيمة الري عند انخفاض الرطوبة
     }
     ```

4. **`GET /api/units/:unitId/status`**
   - الحصول على حالة الوحدة الكاملة (يشمل الإعدادات الحالية)

**إضافة إدارة الوحدات:**
- تخزين بيانات كل وحدة منفصلة
- إدارة إعدادات كل وحدة (قابلة للتعديل من المستخدم):
  - قيمة الري الكمي الافتراضية (لتر)
  - قيمة الري الوقتي الافتراضية (دقيقة)
  - عتبة الرطوبة للري التلقائي (%)
  - نوع الري عند انخفاض الرطوبة (كمي/وقتي)
  - قيمة الري عند انخفاض الرطوبة
- تتبع حالة كل وحدة
- حفظ الإعدادات المخصصة لكل وحدة
- إمكانية تعديل الإعدادات في أي وقت من الواجهة

---

### 3. تحديث واجهة الويب (Frontend)

#### تحديث `public/index.html`:

**إضافة قسم لوحدة الأحواض:**

1. **عرض البيانات:**
   - رطوبة التربة (مؤشر بصري + قيمة)
   - تدفق الماء الحالي
   - إجمالي المياه المستهلكة (اليوم/الأسبوع/الشهر)
   - حالة الري (جاري/متوقف)

2. **واجهة التحكم:**
   - **اختيار نظام الري:**
     - زر: ري كمي
     - زر: ري وقتي
     - زر: ري حسب الرطوبة
     - زر: تحكم يدوي
     - زر: إيقاف
   
   - **إعدادات الري الكمي:**
     - حقل إدخال: الكمية باللتر (قابل للتعديل من المستخدم)
     - زر: حفظ الإعدادات (حفظ كقيمة افتراضية)
     - زر: بدء الري (باستخدام القيمة المدخلة)
     - عرض: القيمة الحالية المحفوظة
   
   - **إعدادات الري الوقتي:**
     - حقل إدخال: المدة بالدقائق (قابل للتعديل من المستخدم)
     - زر: حفظ الإعدادات (حفظ كقيمة افتراضية)
     - زر: بدء الري (باستخدام القيمة المدخلة)
     - عرض: القيمة الحالية المحفوظة
   
   - **إعدادات الري حسب الرطوبة:**
     - حقل إدخال: عتبة الرطوبة (%) (قابل للتعديل من المستخدم)
     - اختيار نوع الري عند انخفاض الرطوبة:
       - ○ ري كمي
       - ○ ري وقتي
     - حقل إدخال: قيمة الري (لتر أو دقيقة حسب الاختيار)
     - زر: حفظ الإعدادات (حفظ كقيمة افتراضية)
     - زر: تفعيل/إلغاء الري التلقائي
     - عرض: الإعدادات الحالية المحفوظة
   
   - **التحكم اليدوي:**
     - زر: تشغيل/إيقاف المضخة
     - زر: فتح القفل
     - زر: إغلاق القفل

3. **رسوم بيانية:**
   - رسم بياني لرطوبة التربة عبر الزمن
   - رسم بياني لاستهلاك المياه

---

## 📁 هيكل الملفات الجديدة

```
firmware/
  └── esp32_plant_bed_unit.ino    (ملف جديد)

server.js                          (تحديث)

public/
  └── index.html                   (تحديث - إضافة قسم الأحواض)
```

---

## 🔧 التفاصيل التقنية

### GPIO Pin Assignments (مقترح):

```
ESP32 Pin Assignments:
- GPIO 4:  حساس رطوبة التربة (Analog) - 3.3V/5V
- GPIO 5:  حساس تدفق الماء (Digital Interrupt) - 12V (متحكم به عبر ريلي 4)
- GPIO 16: SDA (I2C للشاشة)
- GPIO 17: SCL (I2C للشاشة)
- GPIO 18: ريلي المضخة (12V)
- GPIO 19: ريلي فتح القفل (12V)
- GPIO 21: ريلي إغلاق القفل (12V)
- GPIO 22: ريلي حساس التدفق (12V) - Normally Closed (NC)
```

### ملاحظات مهمة حول التوصيلات:

1. **حساس التدفق (Water Flow Sensor):**
   - يعمل على 12V
   - متصل بـ Normally Closed (NC) لريلي 4
   - عندما ريلي 4 مغلق → حساس التدفق يعمل ويقرأ التدفق
   - عندما ريلي 4 مفتوح → حساس التدفق متوقف (لا يستهلك طاقة)
   - الإشارة (Signal) من حساس التدفق متصلة بـ GPIO 5 (Digital Interrupt)

2. **حساس رطوبة التربة (Soil Moisture Sensor):**
   - يعمل على 3.3V أو 5V (من ESP32)
   - متصل بـ GPIO 4 (Analog)
   - لا يحتاج ريلي للتحكم به

3. **الريلايهات:**
   - جميع الريلايهات تعمل على 12V
   - ريلي 4 مهم جداً: يجب تشغيله (إغلاقه) قبل بدء الري لتفعيل حساس التدفق

### منطق التحكم بالقفل:

```cpp
// فتح القفل
void openValve() {
  digitalWrite(VALVE_CLOSE_RELAY, LOW);  // إيقاف ريلي الإغلاق أولاً
  delay(100);
  digitalWrite(VALVE_OPEN_RELAY, HIGH);   // تشغيل ريلي الفتح
  delay(2000);                            // انتظار فتح القفل
  digitalWrite(VALVE_OPEN_RELAY, LOW);   // إيقاف ريلي الفتح (القفل يبقى مفتوحاً)
}

// إغلاق القفل
void closeValve() {
  digitalWrite(VALVE_OPEN_RELAY, LOW);    // إيقاف ريلي الفتح أولاً
  delay(100);
  digitalWrite(VALVE_CLOSE_RELAY, HIGH);  // تشغيل ريلي الإغلاق
  delay(2000);                            // انتظار إغلاق القفل
  digitalWrite(VALVE_CLOSE_RELAY, LOW);   // إيقاف ريلي الإغلاق (القفل يبقى مغلقاً)
}
```

### منطق التحكم بحساس التدفق:

```cpp
// تفعيل حساس التدفق (إغلاق ريلي 4 - NC)
void enableFlowSensor() {
  digitalWrite(FLOW_SENSOR_RELAY, LOW);  // إغلاق الريلي = تفعيل الحساس (NC)
  delay(100);  // انتظار استقرار الحساس
}

// إيقاف حساس التدفق (فتح ريلي 4)
void disableFlowSensor() {
  digitalWrite(FLOW_SENSOR_RELAY, HIGH);  // فتح الريلي = إيقاف الحساس
}

// Interrupt handler لحساس التدفق
volatile int flowPulseCount = 0;

void IRAM_ATTR flowSensorISR() {
  flowPulseCount++;
}

// حساب التدفق (يعتمد على نوع الحساس)
// مثال: YF-S201 ينتج 450 نبضة لكل لتر
float calculateFlowRate() {
  const float PULSES_PER_LITER = 450.0;
  float liters = flowPulseCount / PULSES_PER_LITER;
  flowPulseCount = 0;  // إعادة تعيين
  return liters;
}
```

### تسلسل الري الكمي (محدث):

```cpp
void startQuantitativeIrrigation(float targetLiters) {
  // 1. تفعيل حساس التدفق أولاً
  enableFlowSensor();
  
  // 2. فتح القفل
  openValve();
  
  // 3. تشغيل المضخة
  digitalWrite(PUMP_RELAY, HIGH);
  
  // 4. مراقبة التدفق حتى الوصول للكمية المطلوبة
  float consumedLiters = 0;
  while (consumedLiters < targetLiters) {
    delay(100);  // قراءة كل 100ms
    float currentFlow = calculateFlowRate();
    consumedLiters += currentFlow;
    // تحديث العرض
  }
  
  // 5. إيقاف المضخة
  digitalWrite(PUMP_RELAY, LOW);
  
  // 6. إغلاق القفل
  closeValve();
  
  // 7. إيقاف حساس التدفق (توفير الطاقة)
  disableFlowSensor();
}
```

---

## ✅ قائمة المهام التنفيذية

### المرحلة 1: البرنامج الثابت
- [ ] إنشاء ملف `esp32_plant_bed_unit.ino`
- [ ] إعداد القراءة من حساس رطوبة التربة (3.3V/5V)
- [ ] إعداد القراءة من حساس تدفق الماء (12V عبر ريلي 4)
- [ ] إعداد التحكم بالـ 4 ريلايهات (كلها 12V)
- [ ] تطوير منطق تفعيل/إيقاف حساس التدفق (ريلي 4 - NC)
- [ ] تطوير منطق فتح/إغلاق القفل
- [ ] تطوير نظام الري الكمي (مع تفعيل حساس التدفق)
- [ ] تطوير نظام الري الوقتي (مع تفعيل حساس التدفق)
- [ ] تطوير نظام الري حسب الرطوبة
- [ ] تطوير نظام التحكم اليدوي
- [ ] إضافة الاتصال بالخادم
- [ ] إضافة عرض البيانات على الشاشة المحلية
- [ ] إضافة وضع محلي عند انقطاع الاتصال

### المرحلة 2: تحديث الخادم
- [ ] إضافة إدارة الوحدات في `server.js`
- [ ] إضافة API endpoint لاستقبال بيانات الأحواض
- [ ] إضافة API endpoint لإرسال أوامر التحكم
- [ ] إضافة API endpoint لحفظ الإعدادات (PUT /api/units/:unitId/settings)
- [ ] إضافة منطق إدارة أنظمة الري (مع دعم القيم القابلة للتعديل)
- [ ] إضافة تخزين الإعدادات المخصصة لكل وحدة
- [ ] إضافة تخزين البيانات التاريخية
- [ ] إضافة نظام تتبع الاستهلاك

### المرحلة 3: تحديث الواجهة
- [ ] إضافة قسم عرض بيانات الأحواض
- [ ] إضافة واجهة اختيار نظام الري
- [ ] إضافة واجهة إعدادات الري الكمي (مع إمكانية التعديل والحفظ)
- [ ] إضافة واجهة إعدادات الري الوقتي (مع إمكانية التعديل والحفظ)
- [ ] إضافة واجهة إعدادات الري حسب الرطوبة (مع إمكانية التعديل والحفظ):
  - تعديل عتبة الرطوبة
  - اختيار نوع الري (كمي/وقتي)
  - تعديل قيمة الري
- [ ] إضافة واجهة التحكم اليدوي
- [ ] إضافة عرض القيم المحفوظة الحالية
- [ ] إضافة أزرار حفظ الإعدادات
- [ ] إضافة رسوم بيانية
- [ ] إضافة تحديث فوري للبيانات

### المرحلة 4: الاختبار
- [ ] اختبار القراءة من الحساسات
- [ ] اختبار التحكم بالريلايهات
- [ ] اختبار أنظمة الري المختلفة
- [ ] اختبار الاتصال بالخادم
- [ ] اختبار الواجهة
- [ ] اختبار التكامل الكامل

---

## 🎨 واجهة المستخدم المقترحة

```
┌─────────────────────────────────────────┐
│  وحدة الأحواض الزراعية #1              │
├─────────────────────────────────────────┤
│  رطوبة التربة: [████████░░] 75%        │
│  تدفق الماء: 2.3 لتر/دقيقة              │
│  إجمالي الاستهلاك اليوم: 45.2 لتر      │
│  حالة الري: [متوقف]                    │
├─────────────────────────────────────────┤
│  نظام الري:                             │
│  ○ ري كمي    ○ ري وقتي                 │
│  ○ ري حسب الرطوبة  ○ تحكم يدوي         │
│                                         │
│  [إعدادات الري الكمي]                  │
│  الكمية: [5.0] لتر                      │
│  القيمة المحفوظة: 5.0 لتر              │
│  [حفظ] [بدء الري]                      │
│                                         │
│  [إعدادات الري الوقتي]                  │
│  المدة: [10] دقيقة                      │
│  القيمة المحفوظة: 10 دقيقة             │
│  [حفظ] [بدء الري]                      │
│                                         │
│  [إعدادات الري حسب الرطوبة]            │
│  عتبة الرطوبة: [40]%                    │
│  نوع الري: ○ كمي  ○ وقتي                │
│  قيمة الري: [5.0] لتر/دقيقة            │
│  القيم المحفوظة: 40% | كمي | 5.0 لتر    │
│  [حفظ] [تفعيل] [إلغاء]                 │
│                                         │
│  [التحكم اليدوي]                        │
│  [تشغيل المضخة] [إيقاف المضخة]         │
│  [فتح القفل] [إغلاق القفل]             │
└─────────────────────────────────────────┘
```

---

## ⚡ التوصيلات الكهربائية والسلامة

### توزيع الجهد:
- **12V**: المضخة، القفل (فتح/إغلاق)، حساس التدفق، جميع الريلايهات
- **3.3V/5V**: حساس رطوبة التربة (من ESP32)

### توصيلات الريلايهات:
```
ريلي 1 (المضخة):
  - COM → 12V+
  - NO → المضخة (+)
  - المضخة (-) → GND

ريلي 2 (فتح القفل):
  - COM → 12V+ (مشترك القفل)
  - NO → قفل الفتح (+)
  - قفل الفتح (-) → GND

ريلي 3 (إغلاق القفل):
  - COM → 12V+ (مشترك القفل)
  - NO → قفل الإغلاق (+)
  - قفل الإغلاق (-) → GND

ريلي 4 (حساس التدفق - NC):
  - COM → 12V+
  - NC → حساس التدفق (+)
  - حساس التدفق (-) → GND
  - Signal → GPIO 5 (ESP32)
```

### ملاحظات السلامة:
- استخدام منظم جهد (Voltage Regulator) لـ 12V
- استخدام محولات مستوى الجهد (Level Shifters) إذا لزم الأمر
- تأريض صحيح لجميع المكونات
- حماية من التيار الزائد (Fuses)
- عزل بين دوائر 12V و 3.3V

---

## 📝 ملاحظات مهمة

1. **الأمان:**
   - منع فتح وإغلاق القفل في نفس الوقت
   - حماية من التشغيل المفرط للمضخة
   - كشف انقطاع التدفق (مشكلة في المضخة أو انسداد)
   - تفعيل حساس التدفق قبل بدء الري فقط (توفير الطاقة)
   - إيقاف حساس التدفق بعد انتهاء الري
   - حماية من الجهد العالي (12V) على ESP32

2. **الموثوقية:**
   - وضع محلي عند انقطاع الاتصال
   - حفظ الإعدادات محلياً
   - استئناف تلقائي عند عودة الاتصال
   - معالجة أخطاء الاتصال بشكل صحيح

3. **الدقة:**
   - معايرة حساس التدفق حسب النوع
   - معايرة حساس رطوبة التربة
   - حساب دقيق للكميات
   - مراعاة زمن الاستجابة للحساسات

4. **سهولة الاستخدام:**
   - واجهة واضحة وبسيطة
   - رسائل واضحة للحالة
   - تحديث فوري للبيانات
   - مؤشرات بصرية للحالة

5. **كفاءة الطاقة:**
   - إيقاف حساس التدفق عند عدم الحاجة
   - إدارة ذكية للريلايهات
   - وضع السكون عند عدم الاستخدام

6. **التحكم الكامل من المستخدم:**
   - المستخدم يمكنه تعديل الكمية للري الكمي (باللتر)
   - المستخدم يمكنه تعديل المدة للري الوقتي (بالدقائق)
   - المستخدم يمكنه تعديل عتبة الرطوبة للري التلقائي (%)
   - المستخدم يمكنه اختيار نوع الري عند انخفاض الرطوبة (كمي أو وقتي)
   - المستخدم يمكنه حفظ الإعدادات كقيم افتراضية
   - جميع القيم قابلة للتعديل في أي وقت من الواجهة
   - الإعدادات المحفوظة تُستخدم تلقائياً عند تفعيل النظام

---

## ⏱️ الوقت المتوقع

- البرنامج الثابت: 2-3 أيام
- تحديث الخادم: 1-2 يوم
- تحديث الواجهة: 2-3 أيام
- الاختبار والتكامل: 1-2 يوم

**الإجمالي: 6-10 أيام عمل**

---

**هل توافق على هذه الخطة؟ سأبدأ بالتنفيذ فور موافقتك.**

